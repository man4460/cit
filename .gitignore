# Dependencies
node_modules/

# Build output
backend/dist/
frontend/dist/

# Environment & secrets (ไม่ขึ้น Git)
.env
.env.local
.env.*.local

# SQLite (legacy)
*.db
*.db-journal

# DB backups (อย่าขึ้น Git)
backend/backups/
*.sql

# ไฟล์อัปโหลดจากผู้ใช้ / เอกสารแนบ (ห้ามขึ้น Git)
backend/uploads/
uploads/

# โฟลเดอร์ data ทั้งโปรเจกต์ (ห้ามขึ้น Git)
data/
**/data/

# Logs & OS
*.log
.DS_Store
Thumbs.db

# IDE / เครื่องมือแก้โค้ด
.idea/
.vscode/
.cursor/

# TypeScript build info
**/tsconfig.tsbuildinfo

# Prisma (ถ้ามี client generate นอก node_modules — ปกติอยู่ใน node_modules แล้ว)

# Tesseract trained data (ดาวน์โหลดแยกหรือคัดลอกจากเครื่องติดตั้ง Tesseract)
*.traineddata
