/** @deprecated เมนูอนุมัติ/คำขอถูกตัดแล้ว — คงไฟล์ไว้กัน import เก่า */
export function BudgetSubNav({ className = "" }: { className?: string }) {
  return <nav aria-hidden className={className} />;
}
